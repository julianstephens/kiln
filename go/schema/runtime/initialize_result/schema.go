// Code generated by generategomodels; DO NOT EDIT.

package initialize_result

import (
	"github.com/julianstephens/kiln/go/schema/shared"
	"time"
)

// InitializeResult is generated from a nested JSON Schema object.
type InitializeResult struct {
	// Build information about the build of the runtime.
	Build InitializeResultBuild `json:"build" validate:"required"`
	// CompatibilityMajor the major version of the Kiln runtime protocol that the runtime supports.
	CompatibilityMajor int `json:"compatibility_major" validate:"required,gte=0"`
	// ProtocolVersion the version of the Kiln runtime protocol that the runtime supports.
	ProtocolVersion string `json:"protocol_version" validate:"required,min=1"`
	// Runtime information about the runtime that was initialized.
	Runtime InitializeResultRuntime `json:"runtime" validate:"required"`
	// SchemaSetVersion the version of the Kiln runtime schema set that the runtime supports.
	SchemaSetVersion string `json:"schema_set_version" validate:"required,min=1"`
	// SupportedMethodNamespaces the list of method namespaces that the runtime supports.
	SupportedMethodNamespaces []string `json:"supported_method_namespaces" validate:"required"`
	// SupportedMethods the list of methods that the runtime supports.
	SupportedMethods []string `json:"supported_methods" validate:"required"`
}

// InitializeResultBuild information about the build of the runtime.
type InitializeResultBuild struct {
	// Commit the commit hash of the build.
	Commit string `json:"commit" validate:"required,min=1"`
	// Date the date of the build in ISO 8601 format.
	Date time.Time `json:"date" validate:"required"`
}

// InitializeResultRuntime information about the runtime that was initialized.
type InitializeResultRuntime struct {
	// ID the unique identifier of the runtime.
	ID string `json:"id" validate:"required,min=1"`
	// Name the name of the runtime.
	Name string `json:"name" validate:"required,min=1"`
	// Version the version of the runtime.
	Version string `json:"version" validate:"required,min=1"`
}

func (value InitializeResult) Validate() error {
	return shared.Validate(value)
}
